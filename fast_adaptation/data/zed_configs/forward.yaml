/zed/forward:
  ros__parameters:
    general.grab_frame_rate: 15
    general.grab_resolution: 'HD720'
    general.pub_resolution: 'MEDIUM'
    depth.depth_mode: 'ULTRA'
    depth.openni_depth_mode: false
    sensors.sensors_pub_rate: 50.
    mapping.mapping_enabled: false
    mapping.resolution: 0.1
    depth.depth_confidence: 90